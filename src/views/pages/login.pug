extends ../layouts/master

block title
  title Топ Трафик | Авторизация

block basicSeo
  meta(content="Авторизация страница" name="description")
  meta(content="Авторизация, страница" name="keywords")

block manifestBrowserconfigFiles
  link(href="../manifest.json" rel="manifest" crossorigin)
  meta(content="../browserconfig.xml" name="msapplication-config")

block content
  main