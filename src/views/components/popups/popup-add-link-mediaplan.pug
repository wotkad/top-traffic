.popup__bg(data-popup-name="add-link-mediaplan")
.popup(data-popup-name="add-link-mediaplan")
  .popup__wrapper(data-scroll-lock-scrollable)
    .popup__close
      +button({
        ariaLabel: 'close',
        class: 'button-close popup__hide',
        dataPopupName: 'add-link-mediaplan',
        icon: 'other-close-icon',
        isOnlyIcon: true,
        view: 'button',
      })
    .popup__title URL
    form(action="#")
      .popup__form
        +input({
          isLabel: true,
          isRequired: true,
          label: 'Текст',
          name: 'text',
          placeholder: 'Текст',
          type: 'text',
        })
        +input({
          isLabel: true,
          isRequired: true,
          label: 'URL',
          name: 'url',
          placeholder: 'URL',
          type: 'text',
        })
        +button({
          class: 'button-with-icon-blue lg',
          dataPopupName: 'add-link-mediaplan',
          title: 'Сохранить',
          type: 'submit',
          view: 'button',
        })