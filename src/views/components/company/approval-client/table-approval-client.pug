table.table.table-default.table-approval-client
  thead
    tr
      th(style="min-width: 280px;").fixed-th.fixed-th-border
        .table__sort.flex.gap-x-2.items-center
          p Канал
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Категория
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Администратор
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p.text-right Подписчики
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center.justify-end
          p.text-right Охват
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p.text-right Последняя ставка
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p.text-right Последняя скидка
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center.justify-end
          p.text-right Себестоимость
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center.justify-end
          p.text-right Скидка
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Ближайшая дата поста
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Менеджер
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Согласование клиентом
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Согласование куратором
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Комментарий
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
  tbody
    +subrow-approval-client({
      title: 'Согласованные каналы',
      count: '6',
      class: 'active',
    })
    +row-approval-client({
      isRadio: true,
      accept: true,
      decline: false,
      name: 'approval-client-1'
    })
    +row-approval-client({
      isRadio: true,
      accept: false,
      decline: true,
      name: 'approval-client-2'
    })
    +row-approval-client({
      isRadio: true,
      accept: true,
      decline: false,
      name: 'approval-client-3'
    })
    +row-approval-client({
      isRadio: true,
      accept: false,
      decline: true,
      name: 'approval-client-4'
    })
    +subrow-approval-client({
      title: 'Несогласованные  каналы',
      count: '3',
      class: 'active',
    })
    +row-approval-client({
      isRadio: true,
      accept: true,
      decline: false,
      name: 'approval-client-5'
    })
    +row-approval-client({
      isRadio: true,
      accept: false,
      decline: true,
      name: 'approval-client-6'
    })
    +row-approval-client({
      isRadio: true,
      accept: false,
      decline: true,
      name: 'approval-client-7'
    })
    +subrow-approval-client({
      title: 'Нераспределенные каналы',
      count: '0',
    })