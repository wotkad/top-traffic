table.table.table-default
  thead
    tr
      th
        .table__sort.flex.gap-x-2.items-center
          p Рекламная кампания
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Канал
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Клиент
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Период
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
      th
        .table__sort.flex.gap-x-2.items-center
          p Статус РК
            button(type="button").table__sortbutton
              svg
                use(xlink:href="#other-sort-icon")
  tbody
    +admin-advertising-campaigns-row({
      color: 'status-green',
      status: 'В работу',
    })
    +admin-advertising-campaigns-row({
      color: 'status-blue',
      status: 'В работе',
    })
    +admin-advertising-campaigns-row({
      color: 'status-red',
      status: 'Пауза',
    })
    +admin-advertising-campaigns-row({
      color: 'status-gray',
      status: 'Завершено',
    })
    +admin-advertising-campaigns-row({
      color: 'status-purple',
      status: 'Согласование',
    })
    +admin-advertising-campaigns-row({
      color: 'status-red',
      status: 'Пауза',
    })
    +admin-advertising-campaigns-row({
      color: 'status-gray',
      status: 'Завершено',
    })
    +admin-advertising-campaigns-row({
      color: 'status-purple',
      status: 'Согласование',
    })
    +admin-advertising-campaigns-row({
      color: 'status-purple',
      status: 'Согласование',
    })
    +admin-advertising-campaigns-row({
      color: 'status-blue',
      status: 'В работе',
    })
    +admin-advertising-campaigns-row({
      color: 'status-red',
      status: 'Пауза',
    })
    +admin-advertising-campaigns-row({
      color: 'status-gray',
      status: 'Завершено',
    })
    +admin-advertising-campaigns-row({
      color: 'status-purple',
      status: 'Согласование',
    })
    +admin-advertising-campaigns-row({
      color: 'status-red',
      status: 'Пауза',
    })
    +admin-advertising-campaigns-row({
      color: 'status-gray',
      status: 'Завершено',
    })
    +admin-advertising-campaigns-row({
      color: 'status-purple',
      status: 'Согласование',
    })
    +admin-advertising-campaigns-row({
      color: 'status-purple',
      status: 'Согласование',
    })
    +admin-advertising-campaigns-row({
      color: 'status-red',
      status: 'Пауза',
    })
    +admin-advertising-campaigns-row({
      color: 'status-gray',
      status: 'Завершено',
    })
    +admin-advertising-campaigns-row({
      color: 'status-purple',
      status: 'Согласование',
    })