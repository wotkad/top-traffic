mixin admin-channels-subrow(data)
  tr.subrow(class=data.class)
    td.only-text(colspan="6" style="padding-left: 12px;")
      .fixed-subrow
        .flex.items-center(class="gap-x-2.5")
          h3= data.title
          span= data.count