mixin search(data = {})
  - const { defaults } = require('./_defaults');
  
  form(action="#").search
    label.search__label
      input(class="search__input " + data.class type="search" required autocomplete="on" placeholder="Поиск..." name="search")
      svg
        use(xlink:href="#other-search-icon")